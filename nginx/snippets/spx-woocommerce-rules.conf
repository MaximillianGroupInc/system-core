# Pass the GeoIP2 country code to PHP-FPM / WooCommerce
fastcgi_param HTTP_GEOIP_COUNTRY_CODE $geoip2_data_country_code;

# Optional: Custom header for debugging/WooCommerce plugins
add_header X-Geo-Country $geoip2_data_country_code;

if ($block_reason != "default_none") {
    return 403;
}
